# Displaced Leptons Analysis

(SCAFFOLD -- outline only)

Would contain:

- One-paragraph analysis blurb (signal model, channels)
- Active repos table: `displaced_leptons/` and `DisplacedLeptonsSupplement/`, purpose of each
- Cross-repo relationship note (supplement files consumed by displaced_leptons at runtime),
  pointing down to the repo-level CLAUDE.md for the actual join logic
- FNAL Mount table (all mount points relevant to this analysis, both repos)
- Reference clones (`ref/`) table relevant to this analysis
- EOS: personal subdirectory conventions for this analysis (e.g. supplements/)
- Scratch dirs: notes/, plots/, tmp/
