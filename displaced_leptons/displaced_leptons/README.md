(SCAFFOLD placeholder)

Gitignored in the real setup -- a separate git repo cloned here by setup.py.
Its own CLAUDE.md would outline: PocketCoffea customizations, object selection,
custom processor/configurator, file layout (lib/, configs/, scripts/, etc.).
