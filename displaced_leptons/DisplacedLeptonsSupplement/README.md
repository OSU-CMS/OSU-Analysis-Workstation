(SCAFFOLD placeholder)

Gitignored in the real setup -- a separate git repo cloned here by setup.py.
Would need its own CLAUDE.md outlining: CMSSW package layout, build/run instructions
(doesn't exist yet).
