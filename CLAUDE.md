# [Group] Analysis Workspace

(SCAFFOLD -- outline only)

Would contain:

- One-line blurb: shared home base for the group's LPC analyses, generic tooling only
- Pointer table: analysis name -> directory -> maintainer
- Remote Servers: LPC host/alias/auth conventions (generic)
- FNAL Mount: how to use `fnal-mount.sh`, generic mount mechanics (no per-analysis mount table)
- EOS: redirector convention (generic)
- Style guidelines: TBD if group-wide or per-analysis
- Setup: pointer to `scripts/setup.sh`
