#!/usr/bin/env python3
# SCAFFOLD -- not implemented. Would:
#   - ask for LPC username and grid proxy path -> write to .mount-config.local.sh (gitignored)
#   - ask which analyses to work on -> git clone their repo(s) into <analysis>/<repo>/
#   - ask whether sshfs mounting works on this machine -> record it somewhere Claude can check
